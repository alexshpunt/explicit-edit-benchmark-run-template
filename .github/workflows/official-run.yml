---
# yamllint disable rule:line-length
name: Run official benchmark

"on":
  workflow_dispatch:
    inputs:
      model:
        description: Exact registered model id
        required: true
        default: openai-codex/gpt-5.6-luna
        type: string
      thinking:
        description: Reasoning level
        required: true
        default: low
        type: choice
        options: ["off", low, medium, high, max]
      task:
        description: Optional task id; empty runs all 226 canonical tasks
        required: false
        default: ""
        type: string
      concurrency:
        description: Concurrency for a full canonical run
        required: false
        default: "10"
        type: string
      adapter:
        description: Adapter id from the benchmark registry
        required: true
        default: pi-default
        type: string
      agent_version:
        description: Exact published agent package version
        required: true
        type: string
      harness_version:
        description: Exact extension version; required only for pi-agent-ide
        required: false
        default: ""
        type: string
      runtime_version:
        description: Exact auxiliary runtime version when required
        required: false
        default: ""
        type: string

permissions:
  actions: read
  contents: read
  id-token: write
  attestations: write
  artifact-metadata: write

jobs:
  run:
    uses: alexshpunt/explicit-edit-benchmark/.github/workflows/official-run.yml@6b8d5cf87b556ce178837f67611d2052f1003e3a
    with:
      model: ${{ inputs.model }}
      thinking: ${{ inputs.thinking }}
      task: ${{ inputs.task }}
      concurrency: ${{ inputs.concurrency }}
      adapter: ${{ inputs.adapter }}
      agent_version: ${{ inputs.agent_version }}
      harness_version: ${{ inputs.harness_version }}
      runtime_version: ${{ inputs.runtime_version }}
      provider: openai-codex
    secrets:
      pi_auth_json: ${{ secrets.PI_AUTH_JSON }}

  submit:
    needs: run
    uses: alexshpunt/explicit-edit-benchmark/.github/workflows/official-submit.yml@64b2f612d1fc9b4b79b11b07d9819c1287c9e98b
    with:
      producer_run_id: ${{ github.run_id }}
      producer_attempt: ${{ github.run_attempt }}
      signer_sha: 6b8d5cf87b556ce178837f67611d2052f1003e3a
      policy_sha: 64b2f612d1fc9b4b79b11b07d9819c1287c9e98b
      dataset_repository: alexshpunt/explicit-edit-benchmark
    secrets:
      hf_token: ${{ secrets.HF_TOKEN }}
