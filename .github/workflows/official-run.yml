---
# yamllint disable rule:line-length
name: Run official benchmark

"on":
  workflow_dispatch:
    inputs:
      model:
        description: Exact registered model id
        required: true
        default: openai-codex/gpt-5.6-luna
        type: string
      thinking:
        description: Reasoning level
        required: true
        default: low
        type: choice
        options: ["off", low, medium, high, max]
      task:
        description: Registered task id; one task is a valid partial run
        required: true
        default: replace-all-10-plain
        type: string
      adapter:
        description: Adapter id from the benchmark registry
        required: true
        default: pi-default
        type: string
      agent_version:
        description: Exact published agent package version
        required: true
        type: string
      harness_version:
        description: Exact extension version; required only for pi-agent-ide
        required: false
        default: ""
        type: string
      runtime_version:
        description: Exact auxiliary runtime version when required
        required: false
        default: ""
        type: string

permissions:
  actions: read
  contents: read
  id-token: write
  attestations: write
  artifact-metadata: write

jobs:
  run:
    uses: alexshpunt/explicit-edit-benchmark/.github/workflows/official-run.yml@001d8b4c0304f1295f68507799775ec770ea9636
    with:
      model: ${{ inputs.model }}
      thinking: ${{ inputs.thinking }}
      task: ${{ inputs.task }}
      adapter: ${{ inputs.adapter }}
      agent_version: ${{ inputs.agent_version }}
      harness_version: ${{ inputs.harness_version }}
      runtime_version: ${{ inputs.runtime_version }}
      provider: openai-codex
    secrets:
      pi_auth_json: ${{ secrets.PI_AUTH_JSON }}

  submit:
    needs: run
    uses: alexshpunt/explicit-edit-benchmark/.github/workflows/official-submit.yml@90983afe2505320ed274a970b927da9d4dd86977
    with:
      producer_run_id: ${{ github.run_id }}
      producer_attempt: ${{ github.run_attempt }}
      signer_sha: 001d8b4c0304f1295f68507799775ec770ea9636
      policy_sha: 90983afe2505320ed274a970b927da9d4dd86977
      dataset_repository: alexshpunt/explicit-edit-benchmark
    secrets:
      hf_token: ${{ secrets.HF_TOKEN }}
